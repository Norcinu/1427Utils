<UserControl x:Class="PDTUtils.Views.GameSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:PDTUtils"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="1200">
        

    <UserControl.Resources>
        <conv:IsEnglishCulture x:Key="IsEnglish"/>
        <conv:IsSpanishCulture x:Key="IsSpanish"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:ConvertStakeVisibility x:Key="StakeVis"/>
    </UserControl.Resources>
    
    <Grid>
        <!--IsEnabled="{Binding }"-->
        <StackPanel Visibility="{Binding IsEnabled, Converter={StaticResource BoolToVis}}"> <!-- Bind this to see what the machines culture is.  -->
            <ListView ItemsSource="{Binding Path=Settings}"  Width="1050" HorizontalContentAlignment="Center">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Game Name" Width="150" DisplayMemberBinding="{Binding Path=Title}"/>
                        <GridViewColumn Header="Active" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding Active}"></CheckBox>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        <GridViewColumn Header="Set Stakes" Width="275">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Expander Header="Staking Options">
                                        <StackPanel Margin="10,4,10,4">
                                            <StackPanel Orientation="Horizontal">
                                                <!-- need a converter for binding visibilty to whether stakeone is zero and culture == uk. if spain/region then display -->
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeOne, StringFormat=£{0:#,#.00}}"/> 
                                                </CheckBox>
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeTwo, StringFormat=£{0:#,#.00}}"/>
                                                </CheckBox>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeThree, StringFormat=£{0:#,#.00}}"/>
                                                </CheckBox>
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeFour, StringFormat=£{0:#,#.00}}"/>
                                                </CheckBox>
                                            </StackPanel>
                                        </StackPanel>
                                    </Expander>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        
                        <GridViewColumn Header="Promo" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding StakeFive}"></CheckBox>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <Button Content="Load Game Settings" Width="150" Height="35" Command="{Binding SetGameOptions}" />
            <Button Content="Save Changes" Width="150" Height="35" Command="{Binding SaveGameOptions}" />
        </StackPanel>
        
        <!-- Spanish/Regional Options -->
        <StackPanel> 
            <ListView ItemsSource="{Binding Path=Settings}"  Width="1050" HorizontalContentAlignment="Center">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Game Name" Width="150" DisplayMemberBinding="{Binding Path=Title}"/>
                        <GridViewColumn Header="Active" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding Active}"></CheckBox>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Set Stakes"><!--Width="275"-->
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Expander Header="Staking Options" MinWidth="300">
                                        <StackPanel Margin="10,4,10,4">
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="4" Command="{Binding DataContext.ToggleStake, 
                                                                                RelativeSource={RelativeSource FindAncestor,
                                                                AncestorType={x:Type UserControl}}}">
                                                    
                                                    <!--  Command="{Binding Path=Settings.ToggleStake}" CommandParameter="{Binding IsChecked,
                                                    RelativeSource={RelativeSource self}, Mode=OneWay}"> -->
                                                    <TextBlock Text="{Binding StakeOne, StringFormat=€{0:#,#.00}}"/>
                                                </CheckBox>
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeTwo, StringFormat=€{0:#,#.00}}"/>
                                                </CheckBox>
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeThree, StringFormat=€{0:#,#.00}}"/>
                                                </CheckBox>
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeFour, StringFormat=€{0:#,#.00}}"/>
                                                </CheckBox>
                                                <CheckBox Margin="4" IsChecked="">
                                                    <TextBlock Text="{Binding StakeFive, StringFormat=€{0:#,#.00}}"/>
                                                </CheckBox>
                                            </StackPanel>
                                            
                                            <StackPanel Orientation="Horizontal">
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeSix, StringFormat=€{0:#,#.00}}"/>
                                                </CheckBox>
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeSeven, StringFormat=€{0:#,#.00}}"/>
                                                </CheckBox>
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeEight, StringFormat=€{0:#,#.00}}"/>
                                                </CheckBox>
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeNine, StringFormat=€{0:#,#.00}}"/>
                                                </CheckBox>
                                                <CheckBox Margin="4">
                                                    <TextBlock Text="{Binding StakeTen, StringFormat=€{0:#,#.00}}"/>
                                                </CheckBox>
                                                <!-- <CheckBox Margin="4" IsEnabled="False" Content="{Binding StakeSix, StringFormat=€{0:#,#.00}}" Visibility="{Binding StakeSix, Converter={StaticResource StakeVis}}"/>
                                                <CheckBox Margin="4" IsEnabled="False" Content="{Binding StakeSeven, StringFormat=€{0:#,#.00}}" Visibility="{Binding StakeSeven, Converter={StaticResource StakeVis}}"/>
                                                <CheckBox Margin="4" IsEnabled="False" Content="{Binding StakeEight, StringFormat=€{0:#,#.00}}" Visibility="{Binding StakeEight, Converter={StaticResource StakeVis}}"/>
                                                <CheckBox Margin="4" IsEnabled="False" Content="{Binding StakeNine, StringFormat=€{0:#,#.00}}" Visibility="{Binding StakeNine, Converter={StaticResource StakeVis}}"/>
                                                <CheckBox Margin="4" IsEnabled="False" Content="{Binding StakeTen, StringFormat=€{0:#,#.00}}" Visibility="{Binding StakeTen, Converter={StaticResource StakeVis}}"/> -->
                                            </StackPanel>
                                        </StackPanel>
                                    </Expander>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                            
                        <GridViewColumn Header="Promo" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding StakeFive}"></CheckBox>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <Button Content="Load Game Settings" Width="150" Height="35" Command="{Binding SetGameOptions}" />
            <Button Content="Save Changes" Width="150" Height="35" Command="{Binding SaveGameOptions}" />

        </StackPanel>
        
        
        
    </Grid>
</UserControl>
