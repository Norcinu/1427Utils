<UserControl x:Class="PDTUtils.Views.HopperAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="755"
			 x:Name="ucHopperConfig"
			 >
    <Grid>
		<TabControl FontSize="18">
			<TabItem Header="Empty" MinWidth="250">
				<StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Stretch">
					<ComboBox x:Name="cmbHoppers" Text="Select Hopper" Width="150" Height="35" VerticalAlignment="Top" FontSize="20" Margin="10" SelectionChanged="cmbHoppers_SelectionChanged">
						<ComboBoxItem>Hopper #1</ComboBoxItem>
						<ComboBoxItem>Hopper #2</ComboBoxItem>
					</ComboBox> 
					<StackPanel HorizontalAlignment="Center">
						<!-- <Label x:Name="labelST" Content="{Binding ElementName=cmbHoppers, Path=SelectedItem}"/> -->
						<TextBlock MinWidth="150" MinHeight="35" Margin="10" Name="txtSelHopper" FontSize="20"/>
						<Button MinWidth="150" Margin="10" FontSize="20">Empty Hopper</Button>
							<!-- DataContext="{Binding SelectedItem, ElementName=cmbHoppers}" 
							Text="{Binding @Value}"></TextBlock> -->
					</StackPanel>
				</StackPanel>
			</TabItem>
			<TabItem Header="Info" MinWidth="250">
				
			</TabItem>
			<TabItem Header="Refill" MinWidth="250" FontSize="20">
				<StackPanel HorizontalAlignment="Center">
					<Label FontSize="28" Background="AliceBlue" Foreground="DarkOrchid" HorizontalAlignment="Center">Refill Hoppers</Label>
					<Label>Total Coins Added: € </Label>
					<Label>Hopper #1 Added: €</Label>
					<Label>Hopper #2 Added: €</Label>
				</StackPanel>
			</TabItem>
		</TabControl> 
    </Grid>
</UserControl>
