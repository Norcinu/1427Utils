<Window x:Class="PDTUtils.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PDTUtils"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Background="{DynamicResource WindowBackgroundBrush}"
        Title="1427 Shell Utils" 
        Height="768" 
        Width="1360" 
        Name="WindowMain" 
        WindowStyle="None" 
        Loaded="WindowMain_Loaded" 
        WindowState="Maximized" 
        Closing="WindowMain_Closing" 
        Foreground="#FF837C7C"  
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterScreen" 
        SizeToContent="Manual">

    <!--<Grid Background="#FFC78585"></Grid>-->
    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Name="ColumnOne" Width="207*" />
            <ColumnDefinition Name="ColumnTwo" Width="571*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Name="RowOne" Height="82*" />
            <RowDefinition Name="RowTwo" Height="479*" />
            <RowDefinition Name="RowThree" Height="52*"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <local:GridColourConversion x:Key="gridColour" />
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
            <LinearGradientBrush x:Key="FunkyBrush">
                <GradientStop Color="Yellow" Offset="0" />
                <GradientStop Color="Navy" Offset="1" />
            </LinearGradientBrush>
        </Grid.Resources>

        <!-- 2nd screenshot uses ColumnSpan and RowSpan -->
        <Label Name="lblTop" 
               Background="#336699"  
               BorderBrush="Black" 
               BorderThickness="2" 
               Grid.ColumnSpan="2" VerticalContentAlignment="Center"></Label>
        <!--LightGoldenrodYellow"-->
        <!-- columnspan="5" -->
        <Label Name="lblLeft" 
               Background="DimGray" 
               BorderBrush="Black" 
               BorderThickness="2" 
               Grid.Row="1" 
               Grid.RowSpan="2" Margin="-5,2,6,-2"></Label>
        
        <!--Background="LightGreen"-->
        <Label Name="lblRight" 
               Background="DimGray" 
               BorderBrush="Black" 
               BorderThickness="2" 
               Grid.Column="1" 
               Grid.Row="1" 
               Margin="0,0,0,47" 
               Grid.RowSpan="2" 
               Style="{Binding}" 
               IsEnabled="True">Blue 1</Label>
        
        <Label Name="lblBottom" 
               Background="#FF336699" 
               BorderBrush="Black" 
               BorderThickness="2" 
               Grid.Row="2" 
               Grid.Column="1" 
               Height="48" 
               VerticalAlignment="Bottom"></Label>
        
        <TabControl Name="settingsTab"
                    TabStripPlacement="Top" 
                    Grid.Column="1" 
                    Grid.Row="1" 
                    Visibility="Hidden" 
                    SelectionChanged="Logfile_SelectionChanged" 
                    IsEnabled="False" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" 
                    Margin="14,28,12,84" 
                    Width="545" 
                    Height="327" 
                    ScrollViewer.HorizontalScrollBarVisibility="Visible" />
        
        <Label Grid.Column="2" 
               Height="39" 
               HorizontalAlignment="Right" 
               Margin="0,12,0,0" 
               Name="lblDoorStatus" 
               VerticalAlignment="Top" 
               Width="275" 
               FontWeight="Bold" FontSize="26" FontFamily="Cambria" Content="Door Status : Closed" />
        
        <StackPanel Grid.Column="1" 
                    Grid.Row="1" 
                    HorizontalAlignment="Center" 
                    Margin="0,6,0,0" 
                    Name="stpButtonPanel" 
                    VerticalAlignment="Top" 
                    Orientation="Horizontal" 
                    ScrollViewer.HorizontalScrollBarVisibility="Visible" />
        
        <Label Height="Auto" 
               HorizontalAlignment="Left" 
               Margin="292,12,0,0" 
               Name="lblUptime" 
               VerticalAlignment="Top" 
               Grid.Column="1" 
               Grid.Row="1" 
               Width="Auto" 
               FontSize="36" 
               IsEnabled="False" 
               Visibility="Collapsed" />
        
        <!-- Logfiles -->
        <TabControl Name="MyLogfiles" VerticalAlignment="Top" Grid.Column="1" Grid.Row="1" IsEnabled="False" Visibility="Hidden">
            <TabItem Header="All"></TabItem>
            <TabItem Header="Errors">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <ListView ItemsSource="{Binding Path=LogController.ErrorLog}" MinHeight="650">
                            <ListView.View>
                                <GridView AllowsColumnReorder="False">
                                    <GridViewColumn Header="Date" Width="300" DisplayMemberBinding="{Binding Path=GameDate}" />
                                    <GridViewColumn Header="Error Code" Width="300" DisplayMemberBinding="{Binding Path=ErrorCode}" />
                                    <GridViewColumn Header="Description" Width="300" DisplayMemberBinding="{Binding Path=Stake}" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Hoppers">
                
            </TabItem>
            <TabItem Header="Games">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <StackPanel  HorizontalAlignment="Left">
                        <ListView ItemsSource="{Binding Path=LogController.PlayedGames}" MinHeight="600">
                            <ListView.View>
                                <GridView AllowsColumnReorder="False">
                                    <GridViewColumn Header="Game" Width="150" DisplayMemberBinding="{Binding Path=GameModel}" />
                                    <GridViewColumn Header="Date" Width="150" DisplayMemberBinding="{Binding Path=GameDate}" />
                                    <GridViewColumn Header="Game Stake" Width="150" DisplayMemberBinding="{Binding Path=Stake}" />
                                    <GridViewColumn Header="Credits Before" Width="150" DisplayMemberBinding="{Binding Path=Credits}" />
                                    <!--<GridViewColumn Header="Credits After" Width="150" DisplayMemberBinding="{Binding Path=CreditAfter}" />-->
                                    <GridViewColumn Header="Win Amount" Width="150" DisplayMemberBinding="{Binding Path=WinAmount}" />
                                 </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <TabItem Header="Wins">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <ListView ItemsSource="{Binding Path=LogController.WinningGames}" MinHeight="600">
                            <ListView.View>
                                <GridView AllowsColumnReorder="False">
                                    <GridViewColumn Header="Date" Width="200" DisplayMemberBinding="{Binding Path=GameDate}" />
                                    <GridViewColumn Header="Description" Width="200" DisplayMemberBinding="{Binding Path=Stake}" />
                                    <GridViewColumn Header="Win Amount" Width="200" DisplayMemberBinding="{Binding Path=WinAmount}" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
        
        <ComboBox Width="100" 
                  Height="35" 
                  Name="MachineIniCategorys" 
                  IsEnabled="False" 
                  Visibility="Hidden"
                  ItemsSource="{Binding GetUniqueCategories}"
                  DisplayMemberPath="Category"
                  SelectedValuePath="Category"
                  SelectedValue="{Binding Category}" 
                  SelectionChanged="MachineIniCategorys_SelectionChanged" 
                  DropDownClosed="MachineIniCategorys_DropDownClosed" />


        <TabControl Name="TabSetup" Grid.Row="1" Grid.Column="1" IsEnabled="False" Visibility="Hidden"
                    Height="{Binding Path=ActualHeight,
                    RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}"
                    Width="{Binding Path=ActualWidth, RelativeSource={RelativeSource Mode=FindAncestor, 
                    AncestorType={x:Type Window}}}">
            <TabItem Header="Machine Ini" MinWidth="100" IsEnabled="{Binding Path=KeyDoorWorker.DoorStatus}" Visibility="{Binding Path=KeyDoorWorker.DoorStatus, Converter={StaticResource BoolToVisConverter}}">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <ListView ItemsSource="{Binding GetMachineIni}" HorizontalContentAlignment="Center">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Width="340" Header="Field" 
                                                    DisplayMemberBinding="{Binding Field}" />
                                    <GridViewColumn Width="340" Header="Value"
                                                    DisplayMemberBinding="{Binding Value}" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <TabItem Header="System Volume" MinWidth="100">
                <StackPanel Name="stpVolume">
                    <Slider Height="50" HorizontalAlignment="Center" IsSnapToTickEnabled="True" Name="MasterVolumeSlider" VerticalAlignment="Center" Width="500"  TickFrequency="5" TickPlacement="Both" Maximum="100" ValueChanged="MasterVolumeSlider_ValueChanged" SmallChange="5" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                        <ProgressBar Height="10" Name="progressBar1" Width="500" Value="{Binding ElementName=MasterVolumeSlider, Path=Value}"/>
                        <TextBox Height="23" HorizontalAlignment="Right" Name="txtVolumeSliderValue" VerticalAlignment="Center" Width="50" Text="{Binding ElementName=MasterVolumeSlider, Path=Value}" IsReadOnly="True"  />
                    </StackPanel>                               
                </StackPanel>
            </TabItem>
            <TabItem Header="Hoppers" MinWidth="100" IsEnabled="{Binding Path=KeyDoorWorker.DoorStatus}" Visibility="{Binding Path=KeyDoorWorker.DoorStatus, Converter={StaticResource BoolToVisConverter}}"></TabItem>
            
            <!-- we need more tab controls inside tab controls. -->
            <!-- and more patches that take for ever to install -->
        </TabControl>
               

        <StackPanel Grid.Row="1" Grid.Column="1" Width="Auto" Name="stpGameStatsView" IsEnabled="{Binding Path=Enabler.GameStatistics}" Visibility="{Binding Path=Enabler.GameStatistics, Converter={StaticResource BoolToVisConverter}}">
            <!-- Wrap this in a dockpanel and set to left hand side. On the right have a graph-->
            <TabControl>
                <TabItem Header="Games" MinWidth="100">
                <ListView Name="MachineIniListView" ItemsSource="{Binding GameStatistics.Games}" Width="700" FontSize="16">
                    <ListView.View>
                        <GridView ColumnHeaderToolTip="Game Stats view" AllowsColumnReorder="False">
                            <GridViewColumn Header="Game Number" Width="Auto" DisplayMemberBinding="{Binding GameNumber}" />
                            <GridViewColumn Header="Icon" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding ImageSource}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Model No." Width="100" DisplayMemberBinding="{Binding ModelNumber}" />
                            <GridViewColumn Header="Bets" Width="100" DisplayMemberBinding="{Binding Bets}"/>
                            <GridViewColumn Header="Wins" Width="100" DisplayMemberBinding="{Binding Wins}"/>
                            <GridViewColumn Header="Percentage" Width="100" DisplayMemberBinding="{Binding Percentage}"/>
                            <GridViewColumn Header="Average Stake" Width="100" DisplayMemberBinding="{Binding AverageStake}" />
                        </GridView>
                    </ListView.View>
                </ListView>
                </TabItem>
                <TabItem Header="{Binding MachineIniListView.SelectedItem}"  MinWidth="100">
                    
                </TabItem>
            </TabControl>
        </StackPanel>

   
        <TabControl Name="tabMeters"  Grid.Row="1" Grid.Column="1" Width="Auto" IsEnabled="{Binding Enabler.Meters}" Visibility="{Binding Path=Enabler.Meters, Converter={StaticResource BoolToVisConverter}}">
            <TabItem Header="LCD Meters">
            <ListBox ItemsSource="{Binding Meters.Me}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding LCDShortTerm}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </TabItem>
            <TabItem Header="Cash Reconcilliation">
                <TabControl Grid.Row="0" Grid.Column="0" MinWidth="50" Height="{Binding Path=ActualHeight,
                        RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}"
                        Width="{Binding Path=ActualWidth, RelativeSource={RelativeSource Mode=FindAncestor, 
                        AncestorType={x:Type Window}}}">
                    <TabItem Header="Cash" MinWidth="150">
                        <DockPanel>
                            <ListBox ItemsSource="{Binding MachineData}" DockPanel.Dock="Left">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <ListBoxItem Content="{Binding Field}" />
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </DockPanel>
                    </TabItem>
                    <TabItem Header="Attendant" MinWidth="100"/>
                    <TabItem Header="Book Levels" MinWidth="150"></TabItem>
                </TabControl>
            </TabItem>
            <!--<TabItem Content></TabItem>-->
        </TabControl>

        <!-- System Info View -->
        <StackPanel Orientation="Horizontal" IsEnabled="{Binding DockSystem.IsEnabled}" Grid.Row="1" Grid.Column="1" Visibility="{Binding Path=Enabler.System, Converter={StaticResource BoolToVisConverter}}" >
            <ListBox IsEnabled="{Binding DockSystem.IsEnabled}" ItemsSource="{Binding MachineData}" FontSize="14" ScrollViewer.CanContentScroll="True"  ScrollViewer.HorizontalScrollBarVisibility="Visible">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <ListBoxItem Content="{Binding Field}"></ListBoxItem>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <ListView DockPanel.Dock="Right" ItemsSource="{Binding Path=GamesList.GamesInfo}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Game" DisplayMemberBinding="{Binding Path=Name}"></GridViewColumn>
                        <GridViewColumn Header="Executable Path" DisplayMemberBinding="{Binding Path=Path}"></GridViewColumn>
                        <GridViewColumn Header="MD5 Status" DisplayMemberBinding="{Binding Path=Hash_code}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Background="{Binding Path=Hash_code, 
                                                            Converter={StaticResource gridColour}}">
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>

                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
       
        <!-- End of System Info View -->
        
        <StackPanel Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Button Content="Hoppers" Height="35" HorizontalAlignment="Stretch" Name="btnHoppers" VerticalAlignment="Stretch" Width="170" Click="btnHoppers_Click" FontSize="22" Background="{StaticResource NormalBrush}" Foreground="#FF042271" />
            <Button Content="Setup" Height="35" HorizontalAlignment="Stretch" Name="btnSetup" VerticalAlignment="Stretch" Width="170" Click="btnSetup_Click" FontSize="22" Background="{StaticResource NormalBrush}" Foreground="#FF042271" />
            <Button Content="Games" Height="35" HorizontalAlignment="Stretch" Name="btnGames" VerticalAlignment="Stretch" Width="170" Click="Games_Click" FontSize="22" Background="{StaticResource NormalBrush}" Foreground="#FF042271" />
            <Button Content="Logfiles" Height="35" HorizontalAlignment="Stretch" Name="btnLogfiles" VerticalAlignment="Stretch" Width="170" Click="btnLogfiles_Click" FontSize="22" Background="{StaticResource NormalBrush}" Foreground="#FF042271" />
            <Button Content="Cash" Height="35" Name="btnCash" Width="170" Click="btnReadMeters_Click" FontSize="22" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{StaticResource NormalBrush}" Foreground="#FF042271" />
            <Button Content="Functional Tests" Height="35" HorizontalAlignment="Stretch" Name="btnFunctionalTests" VerticalAlignment="Stretch" Width="170" Click="btnFunctionalTests_Click" FontSize="22" Background="{StaticResource NormalBrush}" Foreground="#FF042271" />
            <Button Content="Machine Settings" Height="35" Name="btnMachineSettings" VerticalAlignment="Stretch" FontSize="22" Width="170" HorizontalAlignment="Stretch" Background="{StaticResource NormalBrush}" Foreground="#FF042271" />
            <Button Content="Performance" Height="35" HorizontalAlignment="Stretch" Name="button1" VerticalAlignment="Stretch" Width="170" FontSize="22" Background="{StaticResource NormalBrush}" Foreground="#FF042271" />
            <Button Content="System Settings" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="170" Height="35" FontSize="22" Name="btnSystem" Click="btnSystem_Click" Background="{StaticResource NormalBrush}" Foreground="#FF042271" />
        </StackPanel>

        <Button Content="Exit" Height="23" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="54,0,0,12" Name="btnExit" Width="75" Grid.Row="2" Click="btnExit_Click" />
    </Grid>

   
</Window>
